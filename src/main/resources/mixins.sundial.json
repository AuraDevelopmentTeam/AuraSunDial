{
    "required": true,
    "minVersion": "0.8.0",
    "package": "${group}.mixin",
    "refmap": "mixins.${id}.refmap.json",
    "target": "@env(DEFAULT)",
    "compatibilityLevel": "JAVA_8",
    "mixins": [
        "MixinWorldInfo",
        "MixinWorldServer"
    ]
}
